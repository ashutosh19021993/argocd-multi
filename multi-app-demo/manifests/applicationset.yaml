apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: multi-app-multi-cluster
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - appName: payments
            path: multi-app-demo/apps/payments
            namespace: payments
            server: https://app-cluster-control-plane:6443   # child cluster

          - appName: booking
            path: multi-app-demo/apps/booking
            namespace: booking
            server: https://kubernetes.default.svc           # argo's own cluster

          - appName: search
            path: multi-app-demo/apps/search
            namespace: search
            server: https://kubernetes.default.svc           # argo's own cluster
  template:
    metadata:
      name: '{{appName}}'
    spec:
      project: default
      source:
        repoURL: https://github.com/ashutosh19021993/argocd-multi.git
        targetRevision: main
        path: '{{path}}'
        helm:
          valueFiles:
            - values.yaml
      destination:
        server: '{{server}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
